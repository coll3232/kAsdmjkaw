<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-feature android:name="tachiyomi.extension" />

    <application
        android:hasCode="true"
        android:icon="@drawable/icon"
        android:label="toon Ki">

        <!-- Specifies the source class to load -->
        <meta-data
            android:name="tachiyomi.extension.class"
            android:value="eu.kanade.tachiyomi.extension.ko.newtokitoki25.NewToki" />

        <!-- Specifies the extension API version (1.4 is standard) -->
        <meta-data
            android:name="tachiyomi.extension.version"
            android:value="1.4" />

        <!-- NSFW metadata flag (1 for true, 0 for false) -->
        <meta-data
            android:name="tachiyomi.extension.nsfw"
            android:value="1" />

    </application>
</manifest>
